apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: authminion-staging-jenkins
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io  
  labels:
    ENV: staging
spec:
  project: jenkins
  source:
    repoURL: 'https://github.com/tmather99/argocd.git'
    path: k8s/staging/apps/testbed/authminion
    targetRevision: main
    helm:
      values: |-
        - idpUrl: 'https://authminion-staging-jenkins.assistdevtest.com'
  destination:
    name: 'cloudgate@tmather@jedi-staging-jenkins.ap-northeast-1.eksctl.io'
    namespace: default
  syncPolicy:
    syncOptions:
    - CreatNamespace=true
    automated:
      selfHeal: true
